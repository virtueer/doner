# React + TypeScript + Vite

This template provides a setup to get React working in Vite with HMR and Biome for linting and formatting.

## Biome Configuration

Linting and formatting are powered by [Biome](https://biomejs.dev/). You can run:

```bash
pnpm lint      # Run linter
pnpm format    # Format codebase
```
