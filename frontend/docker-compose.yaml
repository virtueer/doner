services:
  doner-frontend:
    build:
      context: .
      args:
        VITE_API_URL: http://doner-backend:3000
    container_name: doner-frontend
    environment:
      - NODE_ENV=production
    restart: always
    networks:
      rwznet:
        ipv4_address: 10.31.13.176

networks:
  rwznet:
    external: true